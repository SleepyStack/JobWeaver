spring:
  application:
    name: jobweaver-worker

  datasource:
    url: jdbc:postgresql://localhost:5432/jobweaver
    username: jobweaver
    password: jobweaver

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: jobweaver-workers

server:
  port: 8082

app:
  kafka:
    topics:
      run-job: run-job
      job-completed: job-completed
      job-failed: job-failed

management:
  endpoints:
    web:
      exposure:
        include: health,info
